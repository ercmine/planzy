import type { CanonicalPlace, MatchResult, NormalizedProviderPlace } from "./types.js";
export declare function findPlaceMatch(normalized: NormalizedProviderPlace, candidates: CanonicalPlace[]): MatchResult;
