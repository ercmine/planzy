import { RetentionPolicy } from "../../retention/policy.js";
import type { BusinessClaimEvidenceRecord, BusinessContactMethodRecord, BusinessManagedPlaceContentRecord, BusinessManagedPlaceImage, BusinessManagedPlaceProfile, BusinessManagedHours, BusinessManagedChangeAuditRecord, BusinessPlaceCategorySuggestion, BusinessPlaceClaimRecord, BusinessPlaceLink, BusinessPlaceMenuServiceCatalog, BusinessTrustAuditEvent, BusinessTrustProfile, ClaimAuditEvent, ClaimStatus, ListClaimsOptions, ListClaimsResult, OfficialBusinessDescription, PlaceBusinessOwnershipRecord } from "./types.js";
import type { VenueClaimStore } from "./store.js";
export declare class MemoryVenueClaimStore implements VenueClaimStore {
    private readonly claims;
    private readonly evidence;
    private readonly ownership;
    private readonly content;
    private readonly auditEvents;
    private readonly managedProfiles;
    private readonly officialDescriptions;
    private readonly categorySuggestions;
    private readonly managedHours;
    private readonly businessLinks;
    private readonly menuServiceCatalogs;
    private readonly businessImages;
    private readonly businessManagedAuditEvents;
    private readonly businessContactMethods;
    private readonly businessTrustProfiles;
    private readonly businessTrustAuditEvents;
    private readonly retentionPolicy;
    constructor(retentionPolicy?: RetentionPolicy);
    createClaim(input: BusinessPlaceClaimRecord): Promise<void>;
    listClaims(opts?: ListClaimsOptions): Promise<ListClaimsResult>;
    getClaimById(claimId: string): Promise<BusinessPlaceClaimRecord | null>;
    updateClaim(claimId: string, patch: Partial<BusinessPlaceClaimRecord>): Promise<BusinessPlaceClaimRecord | null>;
    findClaimByPlaceAndUser(placeId: string, userId: string, statuses?: ClaimStatus[]): Promise<BusinessPlaceClaimRecord | null>;
    addEvidence(input: BusinessClaimEvidenceRecord): Promise<void>;
    listEvidence(claimId: string): Promise<BusinessClaimEvidenceRecord[]>;
    getEvidenceById(evidenceId: string): Promise<BusinessClaimEvidenceRecord | null>;
    updateEvidence(evidenceId: string, patch: Partial<BusinessClaimEvidenceRecord>): Promise<BusinessClaimEvidenceRecord | null>;
    upsertOwnership(input: PlaceBusinessOwnershipRecord): Promise<void>;
    listOwnershipByPlace(placeId: string): Promise<PlaceBusinessOwnershipRecord[]>;
    getOwnershipById(ownershipId: string): Promise<PlaceBusinessOwnershipRecord | null>;
    updateOwnership(ownershipId: string, patch: Partial<PlaceBusinessOwnershipRecord>): Promise<PlaceBusinessOwnershipRecord | null>;
    upsertBusinessContent(input: BusinessManagedPlaceContentRecord): Promise<void>;
    listBusinessContent(placeId: string): Promise<BusinessManagedPlaceContentRecord[]>;
    appendAuditEvent(input: ClaimAuditEvent): Promise<void>;
    listAuditEvents(placeId: string): Promise<ClaimAuditEvent[]>;
    upsertBusinessManagedPlaceProfile(input: BusinessManagedPlaceProfile): Promise<void>;
    getBusinessManagedPlaceProfile(placeId: string): Promise<BusinessManagedPlaceProfile | null>;
    upsertOfficialDescription(input: OfficialBusinessDescription): Promise<void>;
    getOfficialDescription(placeId: string): Promise<OfficialBusinessDescription | null>;
    upsertCategorySuggestion(input: BusinessPlaceCategorySuggestion): Promise<void>;
    listCategorySuggestions(placeId: string): Promise<BusinessPlaceCategorySuggestion[]>;
    upsertManagedHours(input: BusinessManagedHours): Promise<void>;
    getManagedHours(placeId: string): Promise<BusinessManagedHours | null>;
    upsertBusinessLink(input: BusinessPlaceLink): Promise<void>;
    listBusinessLinks(placeId: string): Promise<BusinessPlaceLink[]>;
    upsertMenuServiceCatalog(input: BusinessPlaceMenuServiceCatalog): Promise<void>;
    listMenuServiceCatalogs(placeId: string): Promise<BusinessPlaceMenuServiceCatalog[]>;
    upsertBusinessImage(input: BusinessManagedPlaceImage): Promise<void>;
    listBusinessImages(placeId: string): Promise<BusinessManagedPlaceImage[]>;
    upsertBusinessContactMethod(input: BusinessContactMethodRecord): Promise<void>;
    listBusinessContactMethods(placeId: string): Promise<BusinessContactMethodRecord[]>;
    getBusinessContactMethodById(contactMethodId: string): Promise<BusinessContactMethodRecord | null>;
    upsertBusinessTrustProfile(input: BusinessTrustProfile): Promise<void>;
    getBusinessTrustProfile(placeId: string): Promise<BusinessTrustProfile | null>;
    appendBusinessTrustAuditEvent(input: BusinessTrustAuditEvent): Promise<void>;
    listBusinessTrustAuditEvents(placeId: string): Promise<BusinessTrustAuditEvent[]>;
    appendBusinessManagedAuditEvent(input: BusinessManagedChangeAuditRecord): Promise<void>;
    listBusinessManagedAuditEvents(placeId: string): Promise<BusinessManagedChangeAuditRecord[]>;
    create(input: {
        claimId: string;
        venueId: string;
        contactEmail: string;
        verificationStatus: "pending" | "verified" | "rejected";
        createdAtISO: string;
    }): Promise<void>;
    list(): Promise<{
        claims: Array<{
            claimId: string;
            venueId: string;
            contactEmail: string;
            verificationStatus: "pending" | "verified" | "rejected";
            createdAtISO: string;
            updatedAtISO: string;
        }>;
    }>;
    prune(maxAgeMs?: number, now?: Date): number;
}
