---
interface Props {
  class?: string;
  src?: string;
  alt?: string;
}

const { class: className = 'h-6 w-6', src = '/dryad1.svg', alt = 'Dryad logo' } = Astro.props;
---

<img src={src} alt={alt} class={className} loading="lazy" />
